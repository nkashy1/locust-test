name: Locust summary for locust-test

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: simiotics/locust-action@v1
        with:
          format: yaml
